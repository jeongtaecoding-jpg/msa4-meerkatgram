# 프로젝트 구조 및 레이어 역할

현재 프로젝트는 기능별로 패키지를 분리한 **도메인형(Package-by-Feature)** 디렉터리 구조를 사용하고 있습니다. 

## 📂 디렉터리 구조 개요

```text
src/main/java/com/msa4meerkatgram/
├── domain/             # 비즈니스 로직을 담은 핵심 도메인 영역
│   ├── auth/           # 인증 도메인 (로그인, 회원가입 등)
│   ├── file/           # 파일 관리 도메인 (파일 업로드/다운로드 등)
│   ├── post/           # 게시글 도메인 (게시글 작성, 조회 등)
│   └── user/           # 사용자 도메인 (사용자 정보 조회 등)
│
└── global/             # 전역 설정 및 공통 모듈 영역
    ├── config/         # Spring, JPA 등 전역 설정 (Cors, Web, QueryDSL 등)
    ├── errors/         # 전역 예외 처리 및 커스텀 예외 (GlobalExceptionHandler 등)
    ├── responses/      # API 공통 응답 포맷 정의
    ├── security/       # Spring Security, JWT, 인증 필터, 권한 설정 등
    └── util/           # 애플리케이션 전반에서 사용되는 공통 유틸리티 (로컬 파일 매니저 등)
```

## 🏗️ 각 레이어의 역할 (도메인 내부 구조)

`domain/` 하위의 각 비즈니스 도메인 디렉터리는 역할에 따라 다음과 같은 하위 패키지(레이어)로 나뉘어 구성되어 있습니다:

### 1. `controllers` (프레젠테이션 계층)
- **역할**: 클라이언트의 HTTP 요청(Request)을 받아 처리하고 적절한 응답(Response)을 반환합니다.
- **특징**: 파라미터 검증을 수행하고, 실제 비즈니스 로직은 `services` 계층으로 위임합니다.

### 2. `services` (비즈니스 로직 계층)
- **역할**: 애플리케이션의 핵심 비즈니스 요구사항 및 로직을 구현합니다.
- **특징**: 컨트롤러로부터 전달받은 데이터를 기반으로 데이터 가공 및 처리를 진행합니다. `@Transactional`을 통해 트랜잭션을 관리하며, 필요에 따라 여러 `repositories`를 호출하여 작업을 조율합니다.

### 3. `repositories` (데이터 접근 계층)
- **역할**: 데이터베이스와 직접 통신하여 데이터를 생성, 조회, 수정, 삭제(CRUD)하는 역할을 담당합니다.
- **특징**: Spring Data JPA의 `JpaRepository` 인터페이스를 주로 사용하며, 동적 쿼리나 복잡한 조인이 필요한 경우에는 QueryDSL을 활용한 Custom Repository(예: `PostQueryRepository`)를 구현하여 사용합니다.

### 4. `entities` (도메인 모델 계층)
- **역할**: 데이터베이스의 테이블과 1:1로 매핑되는 영속성 객체 클래스입니다.
- **특징**: 데이터의 상태(필드)와 그 데이터를 조작하는 최소한의 비즈니스 규칙(메서드)을 포함합니다. 엔티티 객체는 다른 계층으로의 이동을 최소화하고, DTO로 변환되어 사용되는 것을 권장합니다.

### 5. `requests` (요청 DTO)
- **역할**: 클라이언트 API 요청 바디(Body)나 파라미터를 담기 위한 Data Transfer Object입니다.
- **특징**: `@NotBlank`, `@Size` 등의 어노테이션을 사용하여 데이터 유효성 검증(Validation) 책임을 가집니다.

### 6. `responses` (응답 DTO)
- **역할**: 클라이언트에게 반환할 응답 데이터를 담기 위한 Data Transfer Object입니다.
- **특징**: 데이터베이스 엔티티 구조를 캡슐화하여 외부에 감추고, 클라이언트가 화면을 그리는 데 필요한 실제 데이터만을 가공해서 전달하는 역할을 합니다.
